---
import type { Property } from '@-backend/data/main';
import { PATHS } from '@-backend/data/paths';
import type { GetStaticPaths } from 'astro';
import Page from './_components/index.astro';

export const getStaticPaths: GetStaticPaths = () => PATHS;

export type Props = { property: Property };

const { property } = Astro.props;
---

<Page {...{property}} />
